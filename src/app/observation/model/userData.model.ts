import { ObservationRequest } from './observationData.model';
import { User } from '../../shared/models/user.model';
export class UserData {
    UserData: User;
    ObservationData: ObservationRequest;
    RepotingManager: User;
}
