import { AlphaNumericLimitedspecialcharactersDirective } from './alpha-numeric-limitedspecialcharacters.directive';

describe('AlphaNumericLimitedspecialcharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphaNumericLimitedspecialcharactersDirective();
    expect(directive).toBeTruthy();
  });
});
